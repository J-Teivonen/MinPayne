using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandCamera : MonoBehaviour
{
    public static GameObject handCamera;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (HandMovementR.itemEquip & HandMovementR.item == 0)
        {
            //handCamera.SetActive(true);
            //Instantiate(handCamera, HandMovementR.handR.position, Quaternion.identity);
        }
    }
}
